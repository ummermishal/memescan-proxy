{
  "name": "memescan-proxy",
  "version": "2.0.0",
  "description": "MEMESCAN proxy — DexScreener + CoinGecko, no API key needed",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js",
    "dev": "nodemon proxy.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
